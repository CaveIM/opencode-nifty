/**
 * GPT-5.5 Jay prompt - focused executor for orchestrator-routed
 * categorized tasks, gated on personal manual QA of the artifact's surface.
 */
export declare function buildGpt55JayPrompt(useTaskSystem: boolean, promptAppend?: string): string;

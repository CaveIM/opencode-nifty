#!/usr/bin/env node
console.error("Nifty Codex: when a user names a Nifty card, start with nifty_run_task. It hydrates description, comments, children, and assignment through the Universal MCP workflow.")
